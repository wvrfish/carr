{
  "name": "carr-backend",
  "version": "1.0.0",
  "description": "Vercel serverless backend for Smartcar integration",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "cookie": "^0.7.2",
    "smartcar": "^10.4.0"
  },
  "devDependencies": {
    "vercel": "^41.0.0"
  }
}
